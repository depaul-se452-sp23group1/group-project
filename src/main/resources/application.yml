spring:
  profiles:
    active: prod
  datasource:
    url: jdbc:mariadb://database-1.cey5rm922ile.us-east-1.rds.amazonaws.com/store
    username: store
    password: depaul1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
server:
  servlet:
    context-path: /store